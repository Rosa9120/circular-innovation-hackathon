import { Box } from "@mui/material"
import { styled } from "@mui/system"

const DashboardBox = styled(Box)(({ theme }) => ({
  backgroundColor: "#fff9f9",
  borderRadius: "1rem",
  boxShadow: "0.10rem 0.15rem 0.07rem 0.075rem rgba(0,0,0, 0.4)",
}))

export default DashboardBox